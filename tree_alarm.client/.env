# Base URL of vms_rec's web_vms backend, used by the Monitor tab (see
# src/monitorviewer/vmsRecFetch.ts) for archive/recording-ranges API calls. Native dev default —
# matches vms_rec's WEB_VMS_PORT (.vscode/launch.json "Backend (.NET)").
VITE_VMS_REC_BASE_URL=http://localhost:5134
